__pycache__/
*.egg-info/
dist/
.venv/
.env
.pytest_cache/
.mypy_cache/
promo/

# 375px screenshots regenerated by tests/test_375px_playwright.py on every run.
# Committing them would churn ~320 KB of binaries through diffs for no review
# value; run the test to produce them locally.
docs/design/375px/

# Demo media (© Gymvisual — app use, not redistribution; see docs/demo-media.md)
*.mp4
*.gif
manifest.json
exercises-dataset/

# Frontend build artifacts (issue #155) — the bundle is built in CI/deploy,
# not committed. See ADR 0004 and frontend/package.json.
frontend/dist/
frontend/node_modules/
frontend/tsconfig.tsbuildinfo
